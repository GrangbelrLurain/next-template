'use client';

import Link from 'next/link';

function SomeContainer() {
  return (
    <div className="pt-40 max-w-screen-sm mx-auto">
      <Link href="/some">template</Link>
    </div>
  );
}

export default SomeContainer;
